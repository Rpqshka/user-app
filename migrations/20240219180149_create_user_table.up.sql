CREATE TABLE users(
                      id VARCHAR(255) PRIMARY KEY,
                      first_name VARCHAR(30),
                      last_name VARCHAR(30),
                      age INT,
                      recording_date BIGINT
)